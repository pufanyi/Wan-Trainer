model_path: storage/models/Wan2.2-I2V-A14B-Diffusers
dataset_json: /mnt/umm/users/pufanyi/workspace/cube/dataset/1_step.json
output_dir: storage/checkpoints/cube_test

num_frames: 81
max_area: 184320  # ~320x576
fps: 16
num_workers: 16

batch_size: 1
gradient_accumulation_steps: 1
num_epochs: 2
learning_rate: 1.0e-5
weight_decay: 0.01
max_grad_norm: 1.0
warmup_steps: 20
save_steps: 500
log_steps: 1
seed: 42

torch_compile: true
ema_decay: 0.9999

wandb_project: wan-i2v
